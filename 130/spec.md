**Migrated to michael-conrad/.opencode#837**

All affected files (`.opencode/hooks/pre-push`, `.opencode/skills/git-workflow/`, `.opencode/tests/behaviors/`) live in the `.opencode` submodule (michael-conrad/.opencode), not in this parent repo. The issue has been recreated on the correct repository.

🤖 Co-authored with AI: OpenCode (ollama-cloud/glm-5)