issue_number: 366
phases:
  - phase: 1
    test_strategy: mixed
    tests:
      - type: verification
        description: "Verify submodule SHA is reachable from origin/main"
        command: "git -C .opencode merge-base --is-ancestor babf358e origin/main && echo 'REACHABLE'"
      - type: verification
        description: "Verify AGENTS.md contains new model reference"
        command: "grep -q 'qwen3.8:27b-256k' AGENTS.md"
      - type: verification
        description: "Verify AGENTS.md no longer contains old model reference"
        command: "grep -q 'qwen3.6:35b-256k' AGENTS.md && echo 'STALE_PRESENT' || echo 'OK'"
    note: "This is a pure administrative sync — no behavioral tests needed. Existing behavioral tests (test-2376-sc*.sh) already exist from the submodule work."
