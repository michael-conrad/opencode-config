issue_number: 366
phase: 1
affected_components:
  - component: ".opencode submodule pointer"
    effect: "Updated from aa2426f3 to babf358e"
    ripple: "Build system resolves babf358e on deploy — requires submodule commit to be reachable"
    scope: "Single pointer line in .opencode file"
  - component: "AGENTS.md"
    effect: "Line 86 model reference string updated"
    ripple: "None — doc reference only, no code uses this string at runtime"
    scope: "Single line change"
