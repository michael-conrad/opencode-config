issue_number: 366
concerns:
  - name: "Submodule pointer management"
    boundary: "Restricted to .opencode file — never touch submodule content"
    phase: 1
    separation: "Distinct from doc changes — different file, different concern"
  - name: "Documentation reference accuracy"
    boundary: "Restricted to AGENTS.md line 86 only — no other doc files in scope"
    phase: 1
    separation: "Distinct from submodule pointer — different file, different concern"
