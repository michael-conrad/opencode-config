**Created in wrong repository.** This spec affects files in the `.opencode/` submodule (michael-conrad/.opencode), not this repo. Recreated in the correct repository.

🤖 OpenCode (ollama-cloud/glm-5.1)