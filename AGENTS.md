# AGENTS.md — opencode-config Repository

This repository holds the agent configuration submodule. All agent rules, guidelines, and skills are in the submodule — not here.

## Repo Identity

Session-init emits `## Repo Information` section with `path: .` for this repo
and `path: .opencode` for the submodule. Use the `## Repo Information` section
to resolve owner/repo/platform for API calls targeting each path.